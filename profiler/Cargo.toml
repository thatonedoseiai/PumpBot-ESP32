[package]
name = "profiler"
version = "0.1.0"
edition = "2024"

[dependencies]
embassy-sync = "0.8.0"
esp-hal = { version = "1.1.1", features = ["esp32s3"] }
esp-println = { version = "0.17.0", features = ["esp32s3"] }

[features]
enable = []
